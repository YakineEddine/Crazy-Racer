class_name Palette
extends RefCounted
## Couleurs partagees menu/HUD (STYLE_GUIDE §1). Que du Color(...) — pas de hex.
## Utilise par les ecrans touches (main_menu, race_hud), pas de balayage global.

const NAVY_TOP := Color(0.1, 0.06, 0.24)
const NAVY_BOTTOM := Color(0.02, 0.02, 0.08)
const GOLD := Color(1, 0.88, 0.3)
const SKY := Color(0.7, 0.9, 1)
const MUTED := Color(0.7, 0.7, 0.8)
const BRAND := Color(1, 0.18, 0.53)
const PANEL := Color(0.12, 0.14, 0.3)
const COIN := Color(1, 0.85, 0.2)
